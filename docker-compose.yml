version: '3.8'

services:
  carecover-copilot:
    build: .
    container_name: carecover_copilot
    ports:
      - "8505:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
