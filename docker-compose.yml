version: '3.8'

services:
  carecover-copilot:
    build: .
    container_name: carecover_copilot_app
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
